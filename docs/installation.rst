============
Installation
============

At the command line::

    $ easy_install k-center-problem

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv k-center-problem
    $ pip install k-center-problem
